version: "3.9"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: sootio
    container_name: sootio
    network_mode: host
    env_file:
      - .env
    environment:
      - NODE_ENV=production
    command: ["npm", "run", "start"]
    restart: unless-stopped
