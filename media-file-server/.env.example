# Usenet File Server Configuration

# API Key for file server authentication (optional)
# Generate a secure random key: openssl rand -hex 32
USENET_API_KEY=your_secret_key_here

# SABnzbd Integration
# URL to SABnzbd API (use host.docker.internal for SABnzbd running on host)
SABNZBD_URL=http://host.docker.internal:8080

# SABnzbd API Key (required for progressive extraction monitoring)
# Find in SABnzbd -> Config -> General -> Security -> API Key
SABNZBD_API_KEY=your_sabnzbd_api_key_here

# Minimum download percentage required before allowing archive extraction
# Default: 80.0 (80%)
# RAR archives require most volumes to be downloaded before extraction can succeed
# Lower values may cause extraction failures, higher values are safer but slower
MIN_DOWNLOAD_PERCENT=80.0
